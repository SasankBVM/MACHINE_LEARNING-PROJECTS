This a Support Vector Machine based Classification algorithm for predicting the Parkinson's disease based on some attributes.
To see the project, first go to any text editor and open ui.py file, then at the terminal type "streamlit run ui.py", 
if you don't have the streamlit then install by "pip install streamlit" or "conda install -c conda-forge streamlit" 
